# 객체

## 객체와 프로퍼티

객체 (Object)

변수에 여러가지 값을 저장 가능

### 객체 만들기

- 객체는 **key**와 **value**를 **콜론(:)**으로 구분하는 프로퍼티들의 집합
    
    값의 이름 (Key) : 값 (Value)
    
    key와 value 한 쌍 → 객체의 **속성(Property)**
    
    - Key = Property Name
    - Value = Property value
    
- 객체 안에 있는 여러 개의 프로퍼티들을 **쉼표(,)**로 구분

```jsx
let myObject = {
	key: value,
	key: value,
	key: value
};

myObject.key; // 점 표기법
myObject['key']; // 대괄호 표기법
```

### Property Name

문자열(String) 형태이지만, 따옴표를 생략하고 작성해도 됨

📌 **Property Name 주의 사항**

1. **첫 번째 글자는 반드시 문자, 밑줄(_), 달러 기호($) 중 하나로 시작해야 함**
2. **띄어쓰기 금지!**
3. **하이픈(-) 금지!**

불가피하게 이런 규칙을 벗어나는 경우 → 반드시 **따옴표로 감싸주어야 함** ex) ‘1stCourse’

### Property Value

**모든 자료형 값**을 사용 가능 → 문자형, 숫자, 불린, null, 심지어 객체 안의 객체도 가능

```jsx
let codeit = {
	name:'코드잇',
	'born Year': 2017, // 띄어쓰기 있어서 ''로 감싸줌
	isVeryNice: true,
	worstCourse: null,
	bestCourse: {  
		title: '자바스크립트 프로그래밍 기초',
		language: 'JavaScript'
	} // 객체 안의 객체도 가능
};
```

객체는 ‘object’ 자료형을 가짐

## 객체에서 데이터 접근하기

객체의 프로퍼티는 **key**를 통해서, 점표기법 혹은 대괄호 표기법으로 접근

### 1. 점 표기법

`objevtName.propertyName`

ex) codeit.name

따옴표를 생략할 수 없는 프로퍼티 네임으로는 접근할 수 없음

### 2. 대괄호 표기법

`objectName['propertyName']`

ex) codeit[’born Year’]

📌 대괄호 안이 **문자열**이야햠

띄어쓰기가 있거나 숫자로 시작하는 프로퍼티에도 접근 가능

프로퍼티 네임을 문자열로 만들 수 있는 어떤 방법이든 사용 가능

- 더하기 기호로 **문자열 연결** 가능
    - codeit[’born’ + ‘ Year’]
- **변수에 담긴 값** 활용 가능
    
    ```jsx
    let propertyName = 'name';
    console.log(codeit[propertyName]); // codeit.name과 동일
    ```
    

- **객체 안의 객체 접근**

→ 프로퍼티 네임을 계속 연결해서 접근

ex) codeit.bestCourse.title

= codeit.bestCourse[’title’] // 점표기법을 사용할 수 없는 경우 대괄호 표기법을 사용해 연결하면 된다.

존재하지 않는 프로퍼티에 접근한다면?

⇒ undefined 출력 (에러 발생이 아님)

## 객체 다루기

### 객체에 프로퍼티 추가

존재하지 않는 프로퍼티 네임에 새로운 값을 할당

```jsx
let myObject = {};
myObject.newName = 'Codeit';
console.log(myObject); // { newName: 'Codeit'} 출력
```

### 객체의 프로퍼티 삭제

`delete` 연산자 사용

```jsx
let myObject = {
	key: 'value',
};

delete myObject.key;

console.log(myObject); // {}출력
```

### in 연산자

`‘propertyName’ in object`

프로퍼티네임 in 객체

```jsx
console.log('name' in codeit); 
console.log(codeit.name ! == undefined); //와 동일하게 프로퍼티의 존재 여부를 확인할 수 있다.
```

**불린** 형태로 값을 리턴

## 객체와 메소드

연관성 있는 여러 함수들을 하나로 묶을 때도 객체 사용

프로퍼티 값으로 함수를 넣음

### 실습

```jsx
let myVoca = {
	addVoca: function (key, value) {
	**myVoca[key]** = value; // 대괄호 표기법 사용해야함
	//점 표기법 사용시 myVoca에 문자 그대로 key라는 프로퍼티 이름을 가진 프로퍼티 값에 접근하는 것과 같은 의미
	},
	deleteVoca: function (key) {
	delete myVoca[key];
	},
	printVoca: function (key) {
	console.log(`"${key}"의 뜻은 "${myVoca[key]}"입니다.`);
```

🚨 **주의** : **파라미터로** 다른 변수에 담긴 값을 가져올 때

→ **대괄호 표기법**을 사용해야함!

- 대괄호 표기법: **변수**로 접근
- 점 표기법: 객체 **프로퍼티**로 접근

✅ 메소드도 프로퍼티! **쉼표로 각 메소드를 구분하자**

## for…in 반복문

변수에 객체의 **프로퍼티 네임**이 전달되어 **객체 내의 프로퍼티 개수만큼 동작**

**대괄호 표기법**을 활용하면 **프로퍼티 값**에도 접근 가능

```jsx
let myObject = {
	'key1': 'value1',
	'key1': 'value2',
	'key1': 'value3',
};

**for** (let key ***in*** myObject) {
	console.log(key);
	console.log(myObject[key]);
}
/* 출력
key1
value1
key2
value2
key3
value3 */
```

### 숫자형(양수) 프로퍼티 네임

```jsx
let myObject = {
	300: '정수',
	1.2: '소수'.
};
```

실제로 사용될 때는 문자열로 형 변환이 됨 (자료형은 string)

대괄호 표기법으로만 접근 가능

`console.log(myObject['300'])`

`consol.log(myObject.300); //Error!`

### 정수형 프로퍼티 네임

객체는 **정수형 프로퍼티 네임을 오름차순 정렬후 나머지 프로퍼티들은 추가한 순서대로 정렬**하는 특징이 있음

따옴표가 있는 경우에도 동일 (ex. ‘1’, ‘2’, ‘3’)

```jsx
let myObject = {
  3: '정수3',
  name: 'codeit',
  1: '정수1',
  birthDay: '2017.5.17',
  2: '정수2',
};

for (let key in myObject) {
  console.log(key);
}

/*출력
1
2
3
name
birthDay 
*/
```

## Date 객체

**내장 객체 (Standard built-in objects)**

자바스크립트가 미리 가지고 있는 객체

### Date 객체 생성

**Date**
`let myDate = **new Date()**;`

console.log(myDate)  myDate **객체를 생성한 순간**이 출력됨

 ex. Thu May 18 2017 00:00:00 GMT+0900 (대한민국 표준시)
     | 요일 | 월 | 일 | 연도 |    시간    |                 시간대                    |

**new Date(milliseconds)**
`let myDate = new Date(1000);`
UTC 기준 1970년 1월 1일 00:00:00에서 1000밀리초(=1초) 지난 객체가 만들어짐

**new Date('YYYY-MM-DD')**
`let myDate = new Date('2017-05-18');` 
시간을 정해주지 않으면 **자정**으로 생성

**new Date('YYYY-MM-DDThh:mm:ss')**
`let myDate = new Date('2017-05-18T19:11:16');`
시간을 지정한 경우에는 해당 시간에 맞는 객체가 생성

**new Date(YYYY, MM, DD, hh, mm, ss, ms);**
연도와 월까지 필수, 나머지는 생략 가능
생략한 경우에는 '1일 0시 0분 0초 0 밀리초'로 처리⇒ **newDate(YYYY, MM, 1, 0, 0, 0, 0);**

🚨**주의**:  **month**는 **0부터 시작**함. 

ex. 4 입력 -> May 생성

### `getTime`메소드

`Date` 객체의 기준 날짜인 1970년 1월 1일 0시 0분 0초에서 몇 밀리초가 지났는지(= 타임스탬프 time stamp)를 확인 가능

```jsx
let someday = new Date(2112, 11, 21);

console.log(someday.getTime());// 4511689200000 출력
```

- **Date** : 일자
    
    ex. 18 → 18일
    
- **Day** : 요일 (일요일부터 0~6)
    
    ex. 4 → 목요일
    

### Date 객체 정보 수정

`set`으로 시작하는 다양한 메소드 사용 

(대괄호로 감싸진 요소들은 선택적 요소)

`setFullYear(year, [month], [date])
setMonth(month, [date])
setDate(date)
setHours(hour, [min], [sec], [ms])
setMinutes(min, [sec], [ms])
setSeconds(sec, [ms])
setMilliseconds(ms)
setTime(milliseconds)`

```jsx
let myDate = new Date(2017, 4, 18, 19, 11, 16);

myDate.setFullYear(2002);
myDate.setMonth(6);
myDate.setDate(20);
```

### 간단한 시간 정보 표현

`toLocaleDateString()`, `toLocaleTimeString()`, `toLocaleString()` 메소드

: 사용자의 브라우저에 설정된 국가의 표기에 맞춰 날짜와 시간을 보여줌

```jsx
let myDate = new Date();

console.log(myDate.toLocaleDateString()); // myDate가 가진 날짜에 대한 정보 (년. 월. 일)
console.log(myDate.toLocaleTimeString()); // myDate가 가진 시간에 대한 정보 (시:분:초)
console.log(myDate.toLocaleString()); // myDate가 가진 날짜와 시간에 대한 정보 (년. 월. 일 시:분:초)
```

### 자동 수정

Date 객체는 범위를 벗어나는 값을 설정하려고 하면 자동으로 날짜를 수정함

```jsx
let myDate = new Date(1988, 0, 32); // 1988년 1월 32일은 없습니다

// 2월 1일로 자동고침 되는걸 확인할 수 있습니다.
console.log(myDate) // Mon Feb 01 1988 00:00:00
```

### **`Date.now()` 메소드**

 이 메소드가 호출된 시점의 타임스탬프를 반환

새로운 객체를 만들지 않아도 현 시점의 날짜 값을 얻어낼 수 있음

`Date.now() === myDate.getTime()`

### Date 객체의 형변환

```jsx
let myDate = new Date(2017, 4, 18);

console.log(typeof myDate); // object
console.log(String(myDate)); // Thu May 18 2017 00:00:00 GMT+0900 (Korean Standard Time)
console.log(Number(myDate)); // 1495033200000
console.log(Boolean(myDate)); // true
```

**number타입**으로 변환

:  getTime() 메소드를 사용한 것과 똑같은 수치의 타임스탬프 값을 얻음

`myDate.getTime() === Number(myDate)`

**Date 객체끼리 사칙연산** 가능→ 두 Date 객체끼리의 **시간차이 계산**

```jsx
let myDate1 = new Date(2017, 4, 18);
let myDate2 = new Date(2017, 4, 19);

let timeDiff = myDate2 - myDate1;
console.log(timeDiff); // 86400000 (ms)
console.log(timeDiff / 1000); // 86400 (sec)
console.log(timeDiff / 1000 / 60) // 1440 (min)
console.log(timeDiff / 1000 / 60 / 60) // 24 (hour)
console.log(timeDiff / 1000 / 60 / 60 / 24) // 1 (date)

```

🚨 **주의** : 두 날짜 간의 차이값이 아니라 **며칠째**인지를 확인하려면 **하루를 더 더해야 함**
