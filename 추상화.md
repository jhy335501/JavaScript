# 추상화

## 할당 연산

**assignment operators**

`=` : 오른쪽에 있는 피연산자를 왼쪽에 있는 피연산자에 할당한다

### 복합 할당 연산자

**Compound assignment operators**

할당연산자와 결합하여 자주 쓰이는 표현을 간략히!

```jsx
//두 줄씩 같은 표현
x = x + 1;		
x += 1;
	
x = x * 2;
x *= 2;
	
x = x % 7;
x %= 7;
```

## 리턴문

### 리턴문의 역할

1. 결괏값을 돌려줌
2. **함수의 실행을 중단**
    
    리턴문을 통해 함수호출 부분에 어떤 값을 되돌려주고 나면 그대로 함수의 실행이 중단됨
    
    → 리턴문 아래 있는 코드들(= **Dead Code**)은 절대로 실행될 일이 없음
    

```jsx
function square(x){
	console.log('return 전');
	**return x * x;**
	console.log('return 후'); // Dead Code
}

console.log('함수 호출 전');
console.log(square(3));
console.log('함수 호출 후');

```

### return과 console.log의 차이

```jsx
function printSquare(x) {
	console.log(x * x);
}

function getSquare(x) {
	return x * x;
}

printSquare(3); // 9 출력
getSquare(3); // 출력x
console.log(getSquare(3)); // 9 출력 
console.log(printSquare(3)); // undefined 출력
// 함수를 작성할 때 리턴문을 작성하지 않으면 undefined 값을 리턴함
```

return : 함수를 실행하고 어떠한 값을 돌려주는 것

console.log : 콘솔에 어떤 것을 출력해 주는 함수

## 옵셔널 파라미터

파라미터가 있는 함수에 아무것도 전달 x → `undefined` 값이 나옴

- undefined가 출력되는 경우 정리
    1. 아무런 값도 없는 변수를 출력
    2. 리턴문이 없는 함수를 호출
    3. 파라미터가 있는 함수를 호출할 때 아무런 값도 전달하지 않은 경우

```jsx
function sayHello(name){
	console.log(`안녕하세요 ${name}님!`);
}

let x;
console.log(x); // 1번 경우
console.log(sayHello('코드잇 최고')); // 2번 경우
sayHello(); // 3번 경우
```

undefined가 아닌 다른 값이 자동으로 전달되게끔 **파라미터의 기본값을 설정**

파라미터를 전달받을 때 전달하거나 생략하거나 ‘**선택적으로**’ 전달 ⇒ **optional** parameter

**함수를 호출할 때 파라미터에 값을 미리 할당함**

🚨 **주의** - **파라미터의 위치**

옵셔널 파라미터는 **가장 뒤쪽으로 선언**해야 한다.

`function introduce(name, **nationality = '한국'**, age)` ⇒ **x**

`function introduce(name, age, **nationality = '한국'**)` ⇒ **0**

## 변수의 scope

모든 변수에는 scope, 유효 범위가 존재

**블록문(Block Statement):** 중괄호 { } 로 감싼 코드 

**로컬 변수, 지역 변수 (local Variable)** : 블록문 내에서 선언된 변수. 블록문 내에서만 유효한 범위를 가짐

**글로벌 변수, 전역 변수 (Global Variable)** : 블록문 밖에서 선언한 변수. 파일 전체 어디에서나 유효한 범위를 가짐

블록문 내에서 변수를 사용 로컬변수가 있는지 찾아서 사용, 없으면 글로벌 변수를 확인

글로벌 변수도 없다면 오류 발생

## 상수

**constant**

절대 변하지 않고 항상 일정한 값

(+참고 아직 값이 정해지지 않은 변수를 선언할 때, 자료형은 정해져 있는 경우 null보다는 **숫자**일 경우 `0`을, **문자**열인 경우 `‘’` 빈문자열을 할당해주면 좋다)

### **선언**

`let` 대신에 **`const`**라는 키워드 사용

`const pi = 3.14;`

`const`로 선언한 상수는 **값의 변경이 불가능** 값을 재할당하려고 하면 실행시 오류 발생

### **이름 규칙**

**대문자**와 **밑줄**로 작성 (암묵적 규칙. 상수와 변수 구분 위함)

이름에 들어가는 모든 알파벳을 대문자로 쓰고 두 개 이상의 단어가 있을 때는 밑줄로 두 단어를 구분

`myNumber` **상수로 표현** 

→ `MY_NUMBER`
