# 함수 다루기

## 함수를 만드는 방식

### 함수 선언(Function Declaration)

**`function` 키워드**를 통해서 함수를 선언하는 일반적인 방식

```jsx
function 함수이름(파라미터) {
	동작
	retrun 리턴값
	}
```

**호이스팅**(선언 부분이 끌어올려짐)→  **함수를 선언 이전에 함수를 호출해도 정상적으로 동작, 선언을 하기 전에 호출 가능**

```jsx
printCodeit(); // Codeit이 정상적으로 출력된다

function printCodeit() { // 함수 선언문이 위쪽으로 끌어올려짐 (호이스팅)
	console.log('Codeit');
}
```

**함수 스코프**를 가짐

함수 안에 선언된 함수는 함수 밖에서 호출할 수 없다

```jsx
**function printCodeit()** **{**
	**function printJS()** {
		console.log('JavaScript');
	}
	
	console.log('Codeit');
	printJS();
**}**

printCodeit(); // Codeit JavaScript 출력
printJS(); // ReferenceError
```

하지만, **함수가 아닌 다른 코드 블록**에서 함수 선언을 하게 되면 모두 전역적으로 호출 가능 

```jsx
const x = 4;

**if (x < 5) {**
	function **printJS()**{
		console.log('JavaScript');
	}
**}** // **if문 안에 printJS 함수 선언**

{
	function printCodeit() {
		console.log('Codeit');
	}
} 

printCodeit(); // Codeit 출력
printJS(); // JavaScript 출력
```

### 함수 표현식(Function Expression)

함수 선언을 **변수에 할당** or  **다른 함수의 아규먼트로** 활용 

(변수에 할당하는 것이 포인트가 아니라 함수 선언을 값처럼 사용하는 방식이라는 게 중요)

```jsx
const printCodeit = function () { // 함수 선언을 변수에 할당
	console.log('Codeit');
};
printCodeit();
	
```

```jsx
const myBtn = document.querySelector('#myBtn');

mybtn.addEventListener('click', function(){
	console.log('button is clicked');
}); // addEventListener의 **두번째 파라미터로 함수를 선언해서 전달**

//결과적으로는 함수 선언을 값처럼 활용한다는 점에서 함수 표현식
```

반드시 **변수가 선언된 이후에 함수를 호출**해야 정상적으로 동작, 선언 이전에 접근을 할 수 없다

(호이스팅이 되지 않는다)

```jsx
printCodeit(); // ReferenceError, 선언 전에 접근 불가

const printCodeit = function() { // const로 선언한 변수에 함수를 할당
	console.log('Codeit');
}
```

할당된 **변수의 특성에 따라 스코프가 결정**

- **`var`** 키워드 변수에 할당 → **함수** 스코프
- **`let`** 또는 **`const`** 키워드 변수에 할당 → **블록** 스코프

```jsx
var printJS = function() {
	console.log('JavaScript');
};

let printHi = function() {
	console.log('Hi');
}

const printCodeit = function() {
	console.log('Codeit');
};
```

```jsx
const x = 5; // const라 블록 스코프지만 함수 밖에서 선언됐기에 전역변수-> 어디서든 접근가능

if (x < 5) { // 조건이 거짓 -> 함수가 선언되지 않음
  const printHi = function () {
    console.log('Hi!');
  };
}

printHi();

```

📌 가급적 두 방법 중 하나를 선택해서 최대한 일관된 방식으로 함수를 만드는 것이 좋음

```jsx
{
  function sayHi() {
    console.log('Hi!');
  }
}

sayHi();

// 함수 선언 방식, 함수 스코프
// 코드 블록 안에서 함수를 선언하더라도 블록 밖에서 사용 가능
```

```jsx
sayHi();

function sayHi() {
  console.log('Hi!');
}
// 함수 선언 방식
// 호이스팅으로 인해 함수 선언 전에 호출 가능
```

```jsx
const sayHi = function() {
  console.log('Hi!');
};

sayHi();

// 함수 표현 방식
// 함수 선언 뒤에 호출해서 문제 X
```

```jsx
{
  let sayHi = function () {
    console.log('Hi!');
  };
}

sayHi();
// 함수 표현 방식
// let으로 선언된 변수에 함수를 할당했기 때문에 블록 스코프를 가짐
// 블록 밖에서 sayHi() 호출 시 ReferenceError
```

## 기명함수 표현식

**Named Function Expression**

**함수표현식**으로 함수를 만들 때 함수에 **이름을 지정 가능**

일반적으로 **함수 내부에서 함수 자체를 가리킬 때** 사용됨

- 이름이 없는 함수를 변수에 할당 → name 프로퍼티는 **변수 이름 그 자체**를 문자열로 가짐

```jsx
**const sayHi** = function () {
  console.log('Hi');
};

console.log(**sayHi.name**); // **sayHi**
```

- 함수에 이름을 할당 →  name 프로퍼티는 **함수 이름**을 문자열로 가짐

```jsx
const sayHi = function **printHiInConsole**() {
  console.log('Hi');
};

console.log(**sayHi.name**); // **printHiInConsole**
```

 이 함수 이름은 **함수 내부에서 함수 자체를 가리킬 때** 사용할 수 있고 함수를 외부에서 함수를 호출할 때 사용할 수는 없다

```jsx
const sayHi = function printHiInConsole() {
	console.log('Hi');
};

printHiInConsole(); //ReferenceError
```

📌 함수 내부에서 함수 자신을 사용할 때 → 함수표현식에서는 반드시 기명 함수 표현식을 사용하자!

 ex. 재귀함수

```jsx
let countdown = function printCountdown(n) {
  console.log(n);
  if (n === 0) {
    console.log('End!');
  } else {
    printCountdown(n - 1);
  }
}; // 전달받은 숫자 값이 0이 될 때까지 하나씩 줄여가며 자기자신을 호출하는 함수

let myFunction = countdown;

countdown = null;

myFunction(5); // 정상적으로 동작

```

위의 countdown 함수를 복사하려고 다른 변수에 똑같이 담았다가 countdown 변수에 담긴 값이 변하게 되면 문제 발생

## 즉시 실행 함수

**Immediately Invoked Function Expression (IIFE)**

함수가 선언된 순간에 바로 실행 가능

**함수선언 부분을 소괄호로 감싼 다음에 바로 뒤에 함수를 실행하는 소괄호를 한 번 더 붙여줌**

```jsx
(function () {
  console.log('Hi!');
})();
```

파라미터 작성 가능

```jsx
(function (x, y) {
	console.log(x + y);
})(3, 5);
```

🚨 **주의** :  함수에 이름을 지어주더라도 **외부에서 재사용할 수 없음** → 일반적으로는 이름이 없는 익명 함수를 사용

```jsx
(function sayHi() {
	console.log('Hi!');
})();

sayHi() // ReferenceError
```

다만, 재귀적인 구조를 만들고자 할 땐 이름이 필요할 수도 있음

```jsx
(function countdown(n) {
	console.log(n);
	if(n === 0) {
		console.log('End!');
	} else {
		countdown(n-1);
	}
}){5};
```

### 즉시 실행 함수의 활용

프로그램 **초기화 기능**에 많이 활용

```jsx
(function init() {
  // 프로그램이 실행 될 때 기본적으로 동작할 코드들..
})();
```

재사용이 필요 없는, **일회성** 동작을 구성 

**함수의 리턴값을 바로 변수에 할당 가능**

```jsx
const firstName = 'Young';
const lastName = 'Kang';

const greetingMessage = (function () {
	const fullName = `${firstName} ${lastName}`;
	
	return `Hi! My name is ${fullName}`;
})();
```

## 값으로서 함수

자바스크립트에서 함수는 **객체**이다

함수는 객체 타입의 **값**으로 평가됨

**함수를 호출하는 다양한 방법**

- 함수 표현식으로 선언 후 호출 (**변수**에 할당해서 활용)
    
    ```jsx
    const sayCodeit = function () {
      console.log('Codeit');
    };
    
    sayCodeit();
    ```
    
- **객체 안 메소드** (프로퍼티로 함수 선언 후 프로프티에 접근해서 함수를 호출)
    
    ```jsx
    const codeit = {
      title: 'codeit',
      printTitle: function () {
        console.log('Codeit');
      },
    };
    
    codeit.printTitle();
    ```
    
- **배열의 요소**로 함수 선언 후 배열의 요소에 접근해서 함수 호출
    
    ```jsx
    const codeit = [
      function () {
        console.log('Codeit');
      },
    ];
    
    codeit[0]();
    ```
    
- 다른 함수의 **파라미터로 전달**
    - **콜백 함수 =** 다른 함수의 파라미터로 전달된 함수
    
    ```jsx
    function makeQuiz(quiz, answer, success, fail) {
    	if (prompt(quiz) === answer) {
    		console.log(success());
    	} else {
    		console.lof(fail());
    	}
    };
    
    function getSuccess() { **// 콜백함수**
    	return '정답!';
    };
    
    function getFail() { **// 콜백 함수**
    	return '오답!'
    };
    
    const question = '5 + 3 = ?';
    
    makeQuiz(question, '8', getSuccess, getFail);
    ```
    

함수가 하나의 값으로 취급되는 특성 → 어떤 함수의 리턴값이 함수가 될 수 있음

```jsx
function getFunction() { //**고차함수**
  return function () { // 함수를 리턴
    console.log('Codeit');
  }
}

const printCodeit = getFunction();
printCodeit();

```

**고차 함수 (HIgher Order Function)** : 함수를 리턴하는 함수

변수에 호출된 값을 할당해서 활용하거나 

**이중괄호를 사용**해서 고차함수로 리턴되는 함수를 바로 호출 가능  

```jsx
function getPrintHi() { 
  return function () { 
    console.log('Codeit');
  }
}

getPrintHi**()(); // 이중괄호로 바로 호출**
```

🚨 **주의** : `const printCodeit = getFunction;` 는 안됨

`printCodeit`이라는 변수에 `getFunction`을 그대로 할당하면 

콘솔에 Codeit이라는 문자열을 출력하는 게 아니라

콘솔에 Codeit이라는 문자열을 출력하는 함수를 리턴하게 됨  

(콘솔에는 아무것도 출력되지 않음)

`const printCodeit = getFunction();`처럼 **`getFunction`을 호출해서** 그 리턴 값을 `printCodeit`에 할당해주어야 함

### 일급 함수 (First Class Function)

**변수**나 다른 **데이터 구조 안에 할당**될 수 있음

다른 함수의 **파라미터**로 전달될 수 있음

다른 함수의 **리턴값**이 될 수 있음

📌 자바스크립트는 일급 함수를 가진 프로그래밍 언어이다

## 파라미터 (Parameter)

### 파라미터와 아규먼트의 구분

파라미터(Prameter) : 외부로부터 값을 전달받기 위해 함수를 선언할 때 소괄호 안에 작성 되는 것

아규먼트(Argument) : 함수를 호출할 때 파라미터로 전달하는 값에 해당하는 부분

```jsx
function greeting(name) { // name : 파라미터
	console.log(`Hi! My name is ${name}!`);
}

greeting('JavaScript') // 'JavaScript' : 아규먼트
```

### 파라미터의 기본값

파라미터가 있는 함수를 호출할 때 아무 아규먼트도 전달하지 않음 → 파라미터는 `undefined` 값을 가짐

`Hi! My name is undefined!`  출력

파라미터에 기본값을 할당해뒀다면 그 기본값을 가지게 됨

`Hi! My name is Codeit!` 출력

```jsx
function greeting (name = 'Codeit') {
	console.log(`Hi! My name is ${name}!`);
}

greeting();
```

하나 이상의 파라미터가 존재하는 경우, 아규먼트를 부분적으로 생략하게 된다면?

```jsx
function greeting(**name = 'Codeit', interest**) {
	console.log(`Hi! My name is ${name}!`);
	console.log(`I like ${interest}!`);
}

greeting(**'JavaScript'**); // name에 'JavaScript'가 전달되고, interest에는 아무것도 전달되지 않는다.

// Hi! My name is JavaScript!
// I like undefined! 출력
```

파라미터의 기본값과는 전혀 관계 없이, 함수를 호출할 때 작성한 순서 그대로 전달됨

📌 **기본값이 필요한 파라미터는 가급적 오른편에 작성하자!**

파라미터의 기본값은 아규먼트가 생략될 때 뿐만 아니라 아규먼트가 `undefined` 값으로 전달될 때도 동작

`greeting(**undefined**, 'Python');`

⇒  `Hi! My name is **Codeit**!` `I like Python!` 출력됨

undefined값을 전달해서 기본값을 사용할 수 있다!

🚨 **null 값을 아규먼트로 사용**하게 되면 파라미터는 **null 값을 그대로 전달받게 된다.**

`greeting (**null**, 'Python');`

⇒ `Hi! My name is **null**!` `I like Python!` 출력됨

앞쪽에 정의된 파라미터를 활용해서 파라미터의 기본값을 작성할 수 있다.

```jsx
function defaultTest(**x, y = x + 3**) {
	console.log(`x: ${x}`);
	console.log(`y: ${y}`);
};

defaultTest(2); // x: 2  y: 5 출력
//2를 가진 x가 y의 기본값에 활용됨

defaultTest(2,3); // x: 2  y: 3 출력
//두번째 아규먼트가 있다면 그냥 그 값을 사용 
```

## 아규먼트 (Arguments)

파라미터가 3개인 함수에 3개보다 적은 아규먼트를 전달하면 undefined값을 가지게 되고, 3개보다 많은 아규먼트를 전달하면 4번째 아규먼트는 무시된다

아규먼트 개수에 따라 유연하게 동작하는 함수를 만드려면?

→ 함수 내부에서 **`arguments` 객체** 사용

### argumnets 객체

파라미터와 관계없이, **함수를 호출할 때 전달한 아규먼트들이 대괄호 안에 각 요소로 담김**

배열과 비슷한 모양을 하고 있지만, **배열의 메소드는 사용 불가**, 유사배열

- `length` 프로퍼티로 아규먼트 개수 확인 가능
- 인덱싱을 통해 각 아규먼트에 접근 가능
- for…of 문을 통해 반복 작업 가능

```jsx
function printArguments(a,b,c) {
	for (const arg of arguments) {
		console.log(arg);
	}
	console.log(arguments[0]);
	console.log(arguments[1]);
	console.log(arguments.length);
	console.log('------------------');
}

printArguments('Young', 'Mark', 'Koby');
printArguments('Captain');
printArguments('Jayden', 'Scott');
printArguments('Suri', 'Jack', 'Joy', 'Noel');
```

🚨 **파라미터**의 이름, **함수 내부에서의 변수나 함수**의 이름으로 `arguments` **사용불가**

## Rest Parameter

arguments 객체의 불편한 점들(배열 메소드 사용불가, 마지막 두개나 세개나 따로 묶어서 다루기 불편)을 해결 (ES2015 이후)

- **arguments**
    - 항상 **전달된 모든 아규먼트를 다룸**
- **Rest Parameter**
    - 명확하게 구분해서 다루어야 하는 부분: **일반 파라미터**를 사용
    - 유연하게 다룰 수 있는 부분: **Rest Parameter**사용
        
        ⇒ 필요에 따라 아규먼트를 나눠서 부분적으로 유연하게 활용 가능
        

**파라미터 하나를 만들어서 앞에 마침표 세개를 붙여줌**

**arguments 객체 대신 만든 파라미터를 사용**

```jsx
function printArguments(**...args**) {
	for (const arg of **args**) {
		console.log(arg);
	}
};
```

- Rest Parameter는 배열 → **배열의 메소드 자유롭게 사용 가능**

```jsx
function printArguments(...args) {
	console.log(**args.splice**(0, 2)); // (2) ["Young", "Mark"] 출력
	console.log(**arguments.splice**(0,2)); // Uncaught TypeError
};

printArguments('Young', 'Mark', 'Koby');
printArguments('Captain');
printArguments('Jayden', 'Scott');
printArguments('Suri', 'Jack', 'Joy', 'Noel');
```

- **일반 파라미터와 함께 사용 가능**
    
    앞에 정의된 파라미터에 아규먼트를 먼저 할당하고 **나머지 아규먼트를 배열로 묶는 역할**을 함
    
    ```jsx
    function printRank(first, second, ...others) {
    	console.log(`우승: ${first}`);
    	console.log(`준우승: ${second}`);
    	
    	for (const arg of others) {
    		console.log(`참가자: ${arg}`);
    	}
    }
    
    printRank('Phil', 'Won', 'Emma', 'Min', 'Luke');
    //첫번째와 두번째 아규먼트는 각각 일반 파라미터(first와 second)에 전달되고
    //나머지 아규먼트들은 Rest Parameter(others)에 전달된다
    
    //우승: Phil  준우승: Won  참가자: Emma  참가자: Min  참가자: Luke 출력
    ```
    

📌 일반 파라미터와 함께 사용할 때는 반드시 **가장 마지막에 작성**되어야 함

가급적 Rest Parameter를 우선적으로 활용, 불가피할 때 arguments 객체를 활용하는 것을 권장 

## Arrow Function

ES2015에서 새로 등장

기존의 **함수 선언 방식**을 보다 **간결하게** 만들어 주는 문법

### 용도

이름이 없는 **익명** 함수 

⇒ 변수에 할당하거나 다른 함수를 호출할 때 아규먼트로 사용된다

### 만드는 법

> **function 키워드를 지우고 소괄호 오른편에서 화살표 기호 `=>` 표시**
> 

**원래함수**

```jsx
const getTwice = function (number) {
	return number * 2 ;
}; // 함수 표현식

function getTwice(number) {
	return number * 2;
} // 함수 선언식
```

**Arrow Function으로 변형**

> `function` 키워드를 지우고 소괄호 뒤에 `=>` 를 추가
> 

예시 1

```jsx
const getTwice = (number) **=>** {
	return number * 2;
};
```

예시 2

```jsx
//원래 함수 (콜백함수)
const myBtn = document.querySelector('#myBtn');
myBtn.addEventListener('click', function(){
	console.log('button is clicked!');
});

//Arrow Funtion으로 변형
const myBtn = document.querySelector('#myBtn');
myBtn.addEventListener('click', () => {
	console.log('button is clicked!');
});
```

### 간결하게 표현하는 규칙

1. **함수의 파라미터가 하나 뿐일 때**
    
    ```jsx
    const getTwice = (number) => {
      return number * 2;
    };
    ```
    
    파라미터를 감싸는 소괄호 생략 가능
    
    ```jsx
    const getTwice = number => {
      return number * 2;
    };
    ```
    
    🚨 **주의** 파라미터가 **2개 이상**이거나 **없을 때**는 **반드시 소괄호를 작성해야 함**
    
2. **함수 동작 부분이 return문만 있을 때**
    
    ```jsx
    const sum = (a, b) => {
      return a + b;
    };
    ```
    
    return문과 중괄호 생략 가능
    
    ```jsx
    const sum = (a, b) => a + b;
    ```
    
    🚨 **주의1** 
    
    return문 외에 다른 표현들이 더 필요하다면 ( 조건문, 반복문, 변수 값 할당 등) 증괄호와 return키워드 생략 불가
    

🚨 **주의2** 

함수 내부 동작이 return문 하나밖에 없지만 **return값이 객체**인 경우

⇒ 중괄호와 return 키워드 생략 후 **중괄호 바깥에 소괄호를 감싸서** 함수의 동작 부분의 중괄호로  해석되지 않도록 하자

```jsx
const getCodeit = () => { name : 'Codeit' };
//에러 발생

const getCodeit = () => **(**{ name : 'Codeit' }**)**;
```

🚨 **주의 : Arrow Function에는 argumnets 객체가 없다**

→ arguments 객체를 사용하는 함수는 Arrow Function으로 대체가 어렵다

```jsx
//일반함수
function normalFunc() {
	console.log(arguments);
};

//Arrow Function 
const arrowFunc = () => {
	console.log(argument);
};

nolmalFunc(1, 2, 3); 
arrowFunc(1, 2, 3); //ReferenceError
```

## this

> **메소드를 호출한 객체를 가리키는 키워드**
> 

브라우저 안에서 자바스크립트가 동작한다면 전역객체인 Window 객체가 this의 기본값이다

함수 내부에서 주로 사용됨

```jsx
function getFullName() {
	return `${**user**.firstName} ${**user**.lastName}`;
}

const user = {
	firstName: 'Tess',
	lastName: 'Jang',
	getFullName: getFullName,
};

const admin = {
	firstName: 'Alex',
	lastName: 'Kim',
	getFullname: getFullName,
};

console.log(user.getFullName()); // Tess Jang 출력
console.log(admin.getFullName()); // Tess Jang 출력
```

```jsx
function getFullName() {
	return `${this.firstName} ${this.lastName}`;
}

const user = {
	firstName: 'Tess',
	lastName: 'Jang',
	getFullName: getFullName,
};

const admin = {
	firstName: 'Alex',
	lastName: 'Kim',
	getFullname: getFullName,
};

console.log(user.getFullName()); // Tess Jang 출력
console.log(admin.getFullName()); // Alex Kim 출력
```

**특징**

값이 미리 정해져 있는 것이 아니라 상대적으로 **값이 변화**

- this를 그냥 사용 / 함수를 선언하고 선언된 함수를 그냥 호출
    
    → 기본값으로 Window 객체를 가리킴
    

```jsx
console.log(this); // 윈도우 객체 출력

function printThis() {
	console.log(this);
}
printThis(); // 윈도우 객체 출력

const myObj = {
	content: 'myObj',
	printThis: printThis,
};

const otherObj = {
	content: 'otherObj',
	printThis: printThis,
};

myObj.printThis(); // myObj 객체 출력 
otherObj.printThis(); // otherObj 객체 출력
```

### 일반 함수와 Arrow Function의 this를 다루는 차이

```jsx
console.log(this); // 윈도우 객체 출력

**const printThis = () => {
	console.log(this);
} // Arrow Function으로 변경**

printThis(); // 윈도우 객체 출력

const myObj = {
	content: 'myObj',
	printThis: printThis,
};

const otherObj = {
	content: 'otherObj',
	printThis: printThis,
};

myObj.printThis(); // 윈도우 객체 출력 
otherObj.printThis(); // 윈도우 객체 출력

//모든 결과가 Window 객체
```

- **Arrow Function**

: **Arrow Function이 선언되기 직전의 this 값**

- **일반 함수**

: **호출한 대상에 따라** 상대적으로 변화

📌 **객체에 메소드를 만들 때**는 Arrow Function보다는 **일반 함수가 좀 더 권장됨**

```jsx
function printThisTitle() {
  console.log(this.title);
}

const courseA = {
  title: '프로그래밍 기초 in JavaScript',
  printTitle: **printThisTitle**,
};

const courseB = {
  title: '컴퓨터 개론',
  printTitle: **courseA.printTitle**,
};

const courseC = {
  title: '웹 퍼블리싱',
  printTitle: **courseB.printTitle**,
};

courseA.printTitle(); // 프로그래밍 기초 in JavaScript 출력
courseB.printTitle(); // 컴퓨터 개론 출력 
	courseC.printTitle(); // 웹 퍼블리싱 출력 
```

`courseA`, `courseB`, `courseC` 모두 `printThisTitle` 함수를 가져온 것과 같다.
